export interface TreeConfig {
  rotationSpeed: number;
  lightColor: string;
  showSnow: boolean;
  ornamentColor: 'gold' | 'red' | 'silver';
}

export type OrnamentType = 'sphere' | 'box' | 'diamond';
